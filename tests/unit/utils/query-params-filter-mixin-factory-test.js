import queryParamsFilterMixinFactory from 'require-super-in-init-bug/utils/query-params-filter-mixin-factory';
import { module, test } from 'qunit';

module('Unit | Utility | query-params-filter-mixin-factory', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = queryParamsFilterMixinFactory();
    assert.ok(result);
  });
});
